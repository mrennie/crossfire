<project name="remote-client" default="build" basedir=".">

    <target name="build">
        <echo>Nothing happened.</echo>
        <echo>Eventually this might work, but it doesn't yet</echo>
    </target>
</project>
